# Drop your template here

Put your badge template PNG in this folder, named exactly:

    whiff-badge-no-number.png

That's the file this service draws the founder number and hidden code onto —
the same `TEMPLATE_PATH` from your original script. To use a different name or
location, set `BADGE_TEMPLATE_PATH` in Vercel or your local environment.
